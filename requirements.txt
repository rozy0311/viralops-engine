# ViralOps Engine — Dependencies
# EMADS-PR v1.0

# ── LangGraph / LLM ──
langgraph>=0.2.0
langchain-core>=0.3.0
langchain-openai>=0.2.0
openai>=1.50.0

# ── Config ──
pyyaml>=6.0
python-dotenv>=1.0

# ── HTTP / Network ──
httpx>=0.27.0
aiohttp>=3.9.0

# ── Data ──
pydantic>=2.0
structlog>=24.0

# ── Web Dashboard ──
fastapi>=0.115.0
uvicorn>=0.30.0
jinja2>=3.1
python-multipart>=0.0.9

# ── RSS Feed ──
feedparser>=6.0

# ── Media Processing (Image→Video + Music overlay) ──
moviepy>=2.0
Pillow>=10.0

# ── Token counting ──
tiktoken>=0.7.0

# ── Trends (optional — for TrendResearcher) ──
pytrends>=4.9.0

# ── Legacy Sendible UI Automation (DEPRECATED — replaced by Publer) ──
playwright>=1.40.0
playwright-stealth>=1.0.6

# ── Testing ──
pytest>=8.0
pytest-asyncio>=0.23
